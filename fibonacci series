#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */ 
    int a=0,b=1,n,i,f;
    scanf("%d",&n);
   
    for(i=0;i<n;i++)
    {
        f= a+b;
        printf("%d ",a);
        a=b;
        b=f;
    }
    return 0;
}
